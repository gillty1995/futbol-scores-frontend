// Preloader.js
import React from "react";
import "./Preloader.css";

const Preloader = ({ className }) => {
  return <div className={`circle-preloader ${className}`}></div>;
};

export default Preloader;
